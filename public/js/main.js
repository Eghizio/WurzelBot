/*
//SETTING UP A DATABASE!

//Initializing Field objects
class Field
{
  constructor(field = null, plantID = 0, watered = false)
  {
	this.field = field;
  this.plantID = plantID;
	this.watered = watered;
  }
}

arr = [];

for(let i=1; i<=204; i++){
arr.push(new Field(i));
}

db.fields.insert(arr);

//Initializing plants
insert array from plants.jsonp file
*/
